#include <cs50.h>
#include <stdio.h>
#include <ctype.h>

int main(void)
{
    // copy content from one variable to another
    char x = get_string("x: ");
    char y = x;

    // Make it upper to know if it copied correctly
}
#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>

int main(int argc, string argv[])
{
    // Take in a commandline argument and print out is len
    string text = argv[];
    int len = strlen(text);

    printf("%i\n", len);
}
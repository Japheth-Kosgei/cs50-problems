#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

int main (void)
{
    // Do some basic arithmetic
    // Division
    int x = get_int("x: ");
    int y = get_int("y: ");
    int answer = x / y;
    printf("Answer: %i\n", answer);

    // Multiplication
    // Addition
}

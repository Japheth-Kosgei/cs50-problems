#include <cs50.h>
#include <stdio.h>
#include <string.h>
#include <ctype.h>
#include <math.h>

int main (void)
{
    // Do some basic arithmetic
    // Division
    int x = get_int("x: ");
    int y = get_int("y: ");
    float answer = (float) x / y;
    printf("Answer: %f\n", answer);

    // Multiplication
    // Addition
}

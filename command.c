#include <stdio.h>
#include <cs50.h>

int main(int argc, string argv[])
{
    // This function reads a command line argument and prints it out.
    string name = argv[1];
    printf("%c\n", name[0]);
}